# Configuration of the target PCF instance for apps deployment
api-endpoint: replace 
api-username: replace
api-password: replace
api-org: replace
api-space: replace

# Configuration of local S3 bucket where resource and task images are located
s3-access-key-id: replace
s3-bucket: replace
s3-endpoint: replace
s3-secret-access-key: replace

# Configuration of the source code repository
git-source-repo: replace
git-source-branch: replace

# Configuration of the internal Maven repository
mirror-repo: replace
release-repo: replace
snapshot-repo: replace
plugin-release-repo: replace
plugin-snapshot-repo: replace
repo-user: replace
repo-password: replace

# Configuration of the build artifact to deploy
artifact-name: replace
artifact-packaging: replace # jar or war as value